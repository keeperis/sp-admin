## Summary

- describe the change

## Companion repos

- `sp-admin`:
- `sp-api`:
- `sp-ceramics`:
- `sp-yoga`:

## Checks

- [ ] Branch name uses approved prefix (`feature/`, `fix/`, `hotfix/`, `chore/`)
- [ ] Same branch slug is used in companion repos when needed
- [ ] No unrelated files were included from a dirty working tree
- [ ] `npm run biome:check`
- [ ] `npm run build`
- [ ] Release manifest impact for `pp` / `prod` was considered
